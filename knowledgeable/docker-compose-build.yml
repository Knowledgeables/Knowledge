services:
  app:
    image: ghcr.io/knowledgeables/knowledge:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile.prod

  migrations:
    image: ghcr.io/knowledgeables/knowledge-migrations:${IMAGE_TAG:-latest}
    build:
      context: .
      dockerfile: Dockerfile.migrations
